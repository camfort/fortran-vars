-- | Replacement module for 'Language.Fortran.Vars.Eval' that wraps the existing
--   interface to use the strong representation internally.
module Language.Fortran.Vars.FSRepr.Eval where

{-
eval :: SymbolTable -> Expression a -> ExpVal
eval = 

eval' :: SymbolTable -> Expression a -> Either String ExpVal
eval' symt = \case

-}
